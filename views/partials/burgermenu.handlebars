<li>
	{{burger_name}}

	{{#if devoured}}

	<button class="btn btn-danger ate" data-id="{{this.id}}" data-neweaten="{{devoured}}">Delete it!</button>
	{{else}}
	<button class="btn btn-primary eat" data-id="{{this.id}}" data-neweaten="{{devoured}}">Eat it now!</button>
	{{/if}}


</li>