<header>
    <h1>Eat-Da-Burger !!!</h1>
</header>

<div class="container">
    <div class="row">
        <div class="col-4">
            <h2>Ready to Eat:</h2>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                {{>burgermenu devoured=0}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>


        <div class="col-4 middleDiv">
            <img src="https://images.pexels.com/photos/4676441/pexels-photo-4676441.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940"
                class="rounded mx-auto d-block">
            <p> </p>
            <p> </p>

            <h2 align="center">"Make" your burger below!</h2>
        </div>


        <div class="col-4">
            <h2>You ate it!</h2>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                {{>burgermenu devoured=1}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
    <br>
    <form id="create-form">
        <div class="form-group" align="center">
            <label for="burger" class="burgerLabel">Add a Burger:</label>
            <input type="text" id="burger" name="name">
            <button id="create-button" class="btn btn-primary ml-3" type="submit">Add it to the menu</button>
        </div>

    </form>
</div>